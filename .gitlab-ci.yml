stages:
  - compilation
  - validation

wishlist_build:
  stage: compilation
  script:
    - source /home/latomefw/wishlist/venv/bin/activate
    - python python/wishlist.py
  tags:
    - wishlist
  artifacts:
    paths:
      - firmware
      - software

