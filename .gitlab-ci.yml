wishlist_venv:
  stage: environment
  script:
    - python3 -m venv venv
    - source venv/bin/activate
    - pip install -r requirements.txt

wishlist_build:
  stage: build
  script:
    - echo 'Hello'
  tags:
    - wishlist
  artifacts:
    paths:
      - firmware
      - software