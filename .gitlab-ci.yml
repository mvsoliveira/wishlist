wishlist_venv:
  stage: .pre
  script:
    - source /cephfs/setup.sh
    - python3 -m venv venv
    - source venv/bin/activate
    - pip install -r requirements.txt
  tags:
    - wishlist

wishlist_build:
  stage: build
  script:
    - echo 'Hello'
  tags:
    - wishlist
  artifacts:
    paths:
      - firmware
      - software