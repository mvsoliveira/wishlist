wishlist_venv:
  stage: .pre
  script:
    - python3.9 -m venv venv
    - source venv/bin/activate
    - pip3.9 install -r requirements.txt
  tags:
    - wishlist

wishlist_build:
  stage: build
  script:
    - echo 'Hello'
  tags:
    - wishlist
  artifacts:
    paths:
      - firmware
      - software