stages:
  - environment
  - compilation

wishlist_venv:
  stage: environment
  script:
    - python3.9 -m venv venv
    - source venv/bin/activate
    - pip3.9 install -r requirements.txt
  tags:
    - wishlist
  artifacts:
    untracked: true
    expire_in: 1 days

wishlist_build:
  stage: compilation
  dependencies:
    - wishlist_venv
  script:
    - echo 'Hello'
  tags:
    - wishlist
  artifacts:
    paths:
      - firmware
      - software