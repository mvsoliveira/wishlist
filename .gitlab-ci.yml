stages:
  - environment
  - compilation

wishlist_venv:
  stage: environment
  script:
    - python3.9 -m venv venv
    - source venv/bin/activate
    - pip3.9 install -r requirements.txt
  tags:
    - wishlist

wishlist_build:
  stage: compilation
  needs: wishlist_venv
  script:
    - echo 'Hello'
  tags:
    - wishlist
  artifacts:
    paths:
      - firmware
      - software